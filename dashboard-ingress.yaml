apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard-ingress
  namespace: default
spec:
  rules:
  - host: dashboard.com
    http:
      paths:
      - backend:
          serviceName: mongodb-service
          servicePort: 27017